// functions run a set block of code. a chunk of code that performs a set action when called, or invoked.

// needs keyword (the word is literally function)
// then needs name of function (hi) and parenthesis for paramter
// then needs curly braces, where the operations of our function happens

//ex: 

function hi() {
     console.log('HI!');
}

